# Rust
target/
**/*.rs.bk

# JS / web
node_modules/
dist/
*.tsbuildinfo
**/.next/

# Python
__pycache__/
*.pyc

# OS / editors
.DS_Store
*.swp
*.log

# 环境变量(含明文密钥;.env.example 模板可入)
.env
.env.local

# 微信小程序凭据 —— appsecret / 上传密钥严禁入库
# (appid 本身是公开标识,允许 commit;secret / upload key 只放 .env.local)
private.wx*.key
*.wxkey
project.private.config.json

# 运行时数据目录(缓存 / 落盘)
data/
backend/data/

# Claude Code 本地会话/权限(pid 句柄、settings 个人偏好不进 git)
# 项目规则本体 .claude/CLAUDE.md 要跟踪
.claude/*
!.claude/CLAUDE.md
