/*
 * 不完人 · 全局样式 · 0830 版
 *
 * 换掉了整套「宣纸 + 墨 + 石碑 + 朱印」。那四样加起来在说
 * 「我很古老、我很神秘」，而这个产品要说的是「有人在等你回来」。
 * 参照：星露谷物语 / 旅行青蛙 / 动物森友会 —— 暖、圆、有生气。
 * 设计依据见 .claude/design/product-0830.html
 *
 * 变量名暂时沿用旧名(--paper/--ink/...)以免一次动全部页面；
 * 值全部换过。改名列在 0830 落地清单 A 批的后半。
 */

page {
  /* 底与面 —— 从「旧纸」换成「被灯照着的白」：明度 92 带灰 → 99 带暖 */
  --paper:         #FFFCF6;
  --paper-inset:   #F6F0E6;
  --paper-panel:   #FFFFFF;
  /* 字 —— 暖黑而非纯黑。纯黑压在暖底上会发脏、显硬 */
  --ink:           #2B2620;
  --ink-mid:       #584F47;
  /* 【2026-09-01 加深】。原先是 #A69C90，在纸底上只有 2.55:1 ——
     连 WCAG AA（4.5:1）的一半都不到。而它是全 app 的次要文字色:
     「还没填」「1 笔」「0 / 6 枚徽章」、所有空状态的副标、名册上那行
     「小道士 · 掐指算」。后果不只是读不清 —— 2.55:1 让所有次要信息
     看着像禁用，「我的」那一屏五行值全灰，整张表像是暂不可用
     （2026-09-01 五路评审 · 视觉那一路量出来的）。
     【2026-09-02 再加深一档，并且这次把数记对】。上一版写的「3.92:1」
     跟三个底哪一个都对不上 —— 实测 #857B70 压纸 4.05、压白卡 4.15、
     压凹底（--paper-inset）3.66，三个底一个都没到 AA 的 4.5。
     而第三轮评审在镜像上逐节点扫了 33 屏 416 个文本节点:
     **149 个不达标，色值全是这一个**，没有第二个色。它承载的又正好是
     全 app 最小的那几档字（底栏标签 10px、`.says-id` 10.5px、
     `.badge-d` 10px）—— 小字更该达标，不是更不该。

     瓶颈是 `--paper-inset` #F6F0E6（三个底里最暗的那个）。
     #736A5F 压它 4.68，压纸 5.18，压白卡 5.31 —— 三个底都过。
     `--ink-mid` 跟着一起加深到 #584F47（7.81 / 8.00 / 7.06），
     否则次要色一逼近它，三档层级就塌成两档。 */
  --ink-faint:     #736A5F;
  --stone-line:   #E4DACA;
  --stone-line-x: #EFE7DA;
  /* 标签色跟 --ink-faint 同源，一起加深 */
  --stone-label:  #736A5F;

  /* 主色：琥珀。这个产品的核心情绪是「有人在等你回来」,
     最准的意象是黄昏的灯 —— 紫是这一品类的通用配色，用它等于自认是那一类 */
  --amber:        #FF9A3C;
  --amber-deep:   #E8791A;
  --amber-lite:   #FFE7CC;
  /* 【琥珀有两个身份，颜色不能是同一个】（2026-09-01 第二轮评审 · 视觉）。
     上面三个是【结构色】—— 底、描边、投影、渐变站，那是给眼睛定位用的，
     多亮都行。下面两个是【字色】，要按对比度定:

       --amber-text  琥珀色的字压在浅底上。--amber-deep 拿来当字是 2.86:1，
                     全 app 十四处「去看看 ›」「还没填 ›」都是它 ——
                     换成 #A34700 压纸 5.93:1、压 --amber-lite 5.08:1。
       --on-amber    字压在琥珀实心上。白字压 --amber 只有 2.11:1，
                     压 #FFB463 更低（1.76）;#4A2E10 分别是 5.88 和 7.08。

     两个身份共用一个变量，就一定会有一边不合格 —— 这是上一版的实情。 */
  --amber-text:   #A34700;
  --on-amber:     #4A2E10;
  /* 副色：苔绿 = 村子、住进来了、已完成 */
  --moss:         #3FAE7A;
  --moss-lite:    #DFF3E8;
  /* 甜色：桃粉 = 喜欢、心动、二次元的那点甜 */
  --peach:        #FF7A8A;
  --peach-lite:   #FFE4E7;
  /* 【绿和粉也各有一个字色版】，理由同上面的琥珀。
     --moss 压纸 2.45:1、--peach 压纸 2.20:1 —— 而 --peach 正是所有
     `.err` 用的颜色:出错那句话是整屏唯一要读的东西，它却是最看不清的
     （2026-09-01 第二轮评审）。深一档之后仍然是绿是粉，只是读得出来了。 */
  --moss-text:    #237049;
  --peach-text:   #B82D45;
  /* 旧名保留：朱印那一处改指琥珀，免得留下一个没人用的红 */
  --stamp:        #FF9A3C;

  /* 五行提亮。旧的五个压得太暗，木和金在小字号下几乎同色 */
  --wx-mu:        #57C97E;
  --wx-huo:       #FF7A5C;
  --wx-tu:        #E8A33D;
  --wx-jin:       #8FA4C4;
  --wx-shui:      #4FA8E8;
  /* 药丸标签用的浅底/深字 —— 五行不再当文字色，改当标签(对比度稳、色盲友好) */
  --wx-mu-bg:#E6F7EC;   --wx-mu-fg:#2E8B57;
  --wx-huo-bg:#FFEDE8;  --wx-huo-fg:#D8543A;
  --wx-tu-bg:#FDF2DC;   --wx-tu-fg:#B57A15;
  --wx-jin-bg:#EDF1F8;  --wx-jin-fg:#5C7091;
  --wx-shui-bg:#E4F2FD; --wx-shui-fg:#2C7FBE;

  /* 圆与影 —— 从「表格」到「东西」 */
  --r-card: 24rpx;
  --r-btn:  28rpx;
  --sh-card: 0 1rpx 4rpx rgba(43,38,32,.05), 0 10rpx 28rpx rgba(43,38,32,.06);

  background: var(--paper);
  color: var(--ink);
  font-family: "PingFang SC", "Hiragino Sans GB", "Helvetica Neue", "Helvetica", sans-serif;
  font-size: 28rpx;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

/* 宋体砍了 —— 它是这一版里「古风」最强的单一信号。
   干支本来就是行话，再套一层古体等于双重门槛。
   要强调就加字重，不换字族。 */
.serif {
  font-weight: 600;
  letter-spacing: 0.02em;
}

.mono {
  font-family: "SF Mono", "Menlo", "Courier New", monospace;
  letter-spacing: 0.04em;
}

/* label · 微字 uppercase */
/* 字距从 0.32em 收到 0.1em：大字距是「高冷排版」的标志，
   而这一版要亲和 */
.eyebrow {
  font-size: 21rpx;
  font-weight: 600;
  color: var(--stone-label);
  letter-spacing: 0.1em;
}

/* 更小、更重。又大又细不如小一号加粗耐看 */
.title {
  font-size: 46rpx;
  font-weight: 700;
  color: var(--ink);
  line-height: 1.18;
  letter-spacing: -0.01em;
}

.subtitle {
  font-size: 24rpx;
  color: var(--ink-mid);
  letter-spacing: 0.02em;
}

.hr {
  height: 1rpx;
  background: var(--stone-line-x);
  margin: 24rpx 0;
}

/* 卡片：圆角 24rpx + 双层暖阴影，不用边框。
   边框是文档的语言，阴影是物件的语言 */
.card {
  background: var(--paper-panel);
  border-radius: var(--r-card);
  padding: 26rpx 28rpx;
  box-shadow: var(--sh-card);
}

/* 主按钮：琥珀 + 底部 6rpx 实心深色。
   那 6rpx 不是阴影，是【厚度】—— 按下去整个按钮下沉、厚度消失，
   这是游戏按钮的经典做法，也是「有手感」最便宜的一处。 */
button.btn {
  background: var(--amber);
  /* 【深棕，不是白】。白字压在琥珀 #FF9A3C 上只有 2.11:1 ——
     30rpx（15px）按正文档要 4.5:1，连大字档的 3:1 都够不到。
     截图上「问问婆婆」「去支付」的白字整片发虚，像橙色上浮着一层雾，
     而这是全 app 出现最多的一个元素（2026-09-01 第二轮评审量出来的）。
     #4A2E10 压在同一个琥珀上是 5.88:1;顺带它更像星露谷那一档 ——
     暖底深棕字是那类界面的做法，白字是网页的做法。 */
  color: var(--on-amber);
  border: none;
  border-radius: var(--r-btn);
  font-size: 30rpx;
  letter-spacing: 0.02em;
  padding: 22rpx 56rpx;
  line-height: 1.4;
  font-weight: 600;
  box-shadow: 0 6rpx 0 var(--amber-deep), 0 10rpx 24rpx rgba(232,121,26,.26);
  transition: transform .12s cubic-bezier(.34,1.56,.64,1), box-shadow .12s;
}
button.btn::after { border: none; }
button.btn[disabled] { opacity: 0.45; box-shadow: none; }
/* 按下去：下沉 6rpx，厚度归零 —— 松手时那条回弹曲线带一点超调 */
button.btn.btn-hover:not([disabled]) {
  transform: translateY(6rpx);
  box-shadow: 0 0 0 var(--amber-deep), 0 4rpx 10rpx rgba(232,121,26,.2);
}

/* 次要动作。原先是纯白 + 厚度 + 阴影，跟主按钮同尺寸 ——
   在米色底上那是【跳出来】的，视觉重量跟主按钮几乎一样。
   结果是「回去」在八屏上成了屏幕最大的东西，而它是最不重要的动作:
   香那一屏最要紧的是买，可白底的「回去」比橙色的「买这一档」还醒目。

   改成透明底 + 一圈细描边:看得见、点得着，但一眼就知道它次于主按钮。
   不用纯文字链是因为好几屏上它是唯一的按钮（空态的「回去」），
   太轻了就找不着落点。 */
/* 【2026-09-01】次要按钮从「一圈淡线」改成「一块实的面」。
   原先是透明底 + `--stone-line` 描边，而那个边在纸底上只有 1.31:1 ——
   界面部件的边界该有 3:1，它连一半都不到:截图上「回村里」几乎只剩四个字
   飘着，看不出是一颗按钮。
   把边加深到 #D8C9B0（1.54:1）同时给一块 `--paper-inset` 的底 ——
   这一档的可辨识不再靠那圈线，靠的是【它比纸暗一层】这件事本身，
   而这也正是这一类游戏 UI（星露谷 / 动森）的做法:次要动作是一块木牌，
   不是一个线框。文字仍是 `--ink-mid`（5.65:1），够清楚。 */
/* 【两颗按钮要一样高】。上一版给 ghost 加了 2rpx 边框，而 `.btn` 是
   默认的 content-box —— 边框加在盒子外面，于是次按钮比主按钮
   高 2px、宽 2px。实测:问问婆婆 335×43 / 去屋里看看 164×45，
   并排摆着一眼看得出次的那颗大一圈（2026-09-01 五路评审量出来的）。
   主按钮也留一圈透明边，两边就落在同一个盒模型上。 */
button.btn { box-sizing: border-box; border: 2rpx solid transparent; }
button.btn.ghost {
  background: var(--paper-inset);
  color: var(--ink-mid);
  border: 2rpx solid #D8C9B0;
  box-shadow: none;
}
button.btn.ghost.btn-hover:not([disabled]) {
  transform: none;
  border-color: var(--ink-faint);
  color: var(--ink);
}

/* `.stamp`（朱印红）删了。0830 里它被映射成桃粉，而桃粉是「喜欢」那一档 ——
   用它做主按钮的地方（绑定微信）压不住，看着像个促销标签。
   留着一条没人用的按钮样式，只会诱使下一个人用它。 */

/* ── 五行药丸 ──────────────────────────────────────────────
   五行是【内容标签】，第一要务是一眼分得开。
   旧做法是拿五行色当文字色 —— 对比度随背景漂，小字号下木和金几乎同色。
   改成浅底深字的药丸：对比度稳、色盲友好(形状+文字仍可读),
   而且它把「辛亥」这种行话包装成了看得懂的物件，像游戏里的属性徽章。 */
.pill {
  display: inline-block;
  padding: 4rpx 16rpx;
  border-radius: 999rpx;
  font-size: 23rpx;
  font-weight: 600;
  line-height: 1.5;
}
.pill.mu   { background: var(--wx-mu-bg);   color: var(--wx-mu-fg);   }
.pill.huo  { background: var(--wx-huo-bg);  color: var(--wx-huo-fg);  }
.pill.tu   { background: var(--wx-tu-bg);   color: var(--wx-tu-fg);   }
.pill.jin  { background: var(--wx-jin-bg);  color: var(--wx-jin-fg);  }
.pill.shui { background: var(--wx-shui-bg); color: var(--wx-shui-fg); }
.pill.plain{ background: var(--paper-inset); color: var(--ink-mid);   }

/* 五行前景色 · 用 class wx-{五行拼音} 挂大字/chip */
/* 【文字用 -fg 那一套】。`--wx-jin` 这类是【底色】版：金 #8FA4C4、
   水 #4FA8E8 摆在纸色底上对比度只有 2.5:1 左右，而「你缺的是「金」」
   里那个字是这一屏最重要的一个字 —— 看不清就等于没说。
   药丸（`.pill.*`）一直用的就是 -fg，只有这一套落在了底色版上。 */
.wx-mu   { color: var(--wx-mu-fg);   }
.wx-huo  { color: var(--wx-huo-fg);  }
.wx-tu   { color: var(--wx-tu-fg);   }
.wx-jin  { color: var(--wx-jin-fg);  }
.wx-shui { color: var(--wx-shui-fg); }

/* 五行背景色 · 用于分布条 */
.wx-mu-bg   { background: var(--wx-mu);   }
.wx-huo-bg  { background: var(--wx-huo);  }
.wx-tu-bg   { background: var(--wx-tu);   }
.wx-jin-bg  { background: var(--wx-jin);  }
.wx-shui-bg { background: var(--wx-shui); }

.page {
  padding: 48rpx 40rpx 96rpx;
  min-height: 100vh;
  box-sizing: border-box;
}

/* ── 行：键 · 值 · ›（设计册 10.2 的「行」，H2 / M1 / M3 / M5 / P1 / P2 都用）──
   一直没有样式。`me` 那一屏五条入口都挂着 .entry，而全工程没有一条规则
   接住它 —— 于是它们渲成三段贴在一起的行内文字，没有行高、没有分隔、
   点得到的地方只有那几个字本身。不报错，看着也「有内容」。

   高度按【触点】定，不按文字定：88rpx = 44px，是能稳稳点中的下限。
   值那一栏可长可短，行高不许跟着变。 */
/* 0830：从「一条条横线」改成【圆角分组卡片】。
   横线是文档的语言 —— 一屏全是横线，读起来像一张表而不是一个 app。
   卡片一包，行与行之间的分隔线只留在卡片内部，四角圆起来，
   整块浮在暖底上。这是 iOS 设置那一类的做法，但底色和圆角按这一版的调子。 */
.entries {
  background: var(--paper-panel);
  border-radius: var(--r-card);
  box-shadow: var(--sh-card);
  overflow: hidden;
}
.entry {
  display: flex;
  align-items: center;
  min-height: 96rpx;
  padding: 0 26rpx;
  border-bottom: 1rpx solid var(--stone-line-x);
}
/* 最后一行不要分隔线 —— 卡片的下边缘已经在那儿了 */
.entry:last-child { border-bottom: none; }
/* 【客服那一行必须是 button】—— 微信的客服会话只认 `open-type="contact"`，
   而那个属性只在 button 上生效。button 自带一身样式（背景、边框、圆角、
   居中、行高），全部清掉，让它跟旁边两行 view 长得一模一样:
   同一张卡里三行，其中一行因为标签不同而高半格、字偏一点，
   人第一眼看不出为什么，只觉得这里没做完。 */
button.entry-btn {
  width: 100%; margin: 0; padding: 0 26rpx;
  background: transparent; border: none; border-radius: 0;
  font-size: inherit; line-height: inherit; color: inherit;
  text-align: left;
}
button.entry-btn::after { border: none; }
.entry-hover { background: var(--paper-inset); }
.entry-k {
  flex: none;
  font-size: 30rpx;
  color: var(--ink);
  letter-spacing: 0.04em;
}
.entry-v {
  flex: 1;
  margin-left: 24rpx;
  text-align: right;
  font-size: 26rpx;
  color: var(--ink-faint);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.entry-arrow {
  flex: none;
  margin-left: 14rpx;
  font-size: 28rpx;
  /* 【2026-09-01】原先用 --stone-line（1.31:1）—— 那是【线】的颜色，
     当文字色用等于看不见。箭头是「这一行能点进去」的唯一记号。 */
  color: var(--ink-faint);
}
/* 没有下一屏可去的那种：值不是「提示」而是事实，所以颜色跟正文一档，
   也不该有箭头 —— 有箭头而点不动，比没有箭头更糟 */
.entry-flat .entry-v { color: var(--ink-mid); }

/* ── 弹性槽：矮屏收起（设计 10.1）───────────────────────────────
   槽里的东西的判据就是「删掉它这一屏仍然成立」，所以放不下时收起来，
   而不是让整屏滚 —— 用户明确要过：小程序没有上下滑动。

   699px 是量出来的：我家那一屏在内容区 700 以上才装得下「近几次」
   （罗盘已经缩到 132px 下限）。

   为什么不用 JS 量：量完改直径、再量的收敛循环把版式变成了时序问题，
   慢机器上还没落定就被量走，同一份代码本机绿、CI 红（2026-08-23）。

   **这里的 100vh 是真机语义 —— tabBar 是原生的、在页面之外，
   所以 100vh 就是内容区。** 镜像里 tabBar 是文档内的 div，
   同一条规则要加上那 50px，换算写在 `web/runtime/page.js` 的
   `paintTabBar` 里，跟 min-height 那处补偿同一个地方、同一个理由。 */
@media (max-height: 699px) {
  .flexslot { display: none; }
}


/* ── 空状态（0830 新增）──────────────────────────────────────
   空屏是最容易被记住的地方，也是这一版之前最潦草的地方：
   一句灰字贴在顶上，下面四分之三全空。

   三件事：①【居中】，不要挤在顶上 ② 一个图形，让它不像出错
   ③ 一条出路。设计册 10.7 说的「空状态要指得出去处」在这儿落地。 */
.empty-state {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 40rpx 40rpx 80rpx;
}
/* 空状态那个圆牌。
   【2026-09-01】圈里放的从【印刷体汉字】换成【像素画】。
   原先四屏各放一个字（问 / 订 / 单 / ?），而这个产品全身都是像素:
   四十个村民、六间屋、一整幅村子 —— 空态却是排版字，
   而空屏是最容易被记住的地方（五路评审 · 视觉，说这是「游戏感」
   差距最集中的一处，因为最有生气的资产早就画好了）。
   画的都是村子里已经有的东西:签筒、木箱、单据、空门牌。
   底还是那圈琥珀渐变 —— 它是对的，换的只是里头那个东西。 */
.empty-art {
  width: 168rpx; height: 168rpx; border-radius: 999rpx;
  background: linear-gradient(155deg, var(--amber-lite) 0%, #FFF6EA 100%);
  box-shadow: 0 8rpx 24rpx rgba(232,121,26,.12);
  display: flex; align-items: center; justify-content: center;
  font-size: 64rpx; color: var(--amber-text); font-weight: 700;
  margin-bottom: 30rpx;
}
/* 有像素图的那几屏:图压在圆牌上，字就不摆了（wxml 里留空）。
   `pixelated` 是必须的 —— 16×16 放到 84px，不声明就是一团糊。

   【2026-09-01 第二轮评审：这里犯了跟头像底盘一模一样的错】。
   上面 `.empty-art` 的底是用简写 `background:` 写的，简写把渐变塞进
   background-image;这条 `.pix` 再写一次 background-image，就把那圈琥珀
   整个顶掉了 —— 四屏的圆牌只剩一圈 box-shadow，里头是纸色。
   取样为证:ask 屏圆牌横向中线上全是 #FFFCF6。
   同一个坑我今天上午刚在 `.face-*` 上修过，写这一条时又踩了一次。
   办法:两层一起写在 background-image 里（图在上、渐变在下），
   再补一个 background-color 兜底。 */
.empty-art.pix {
  background-color: var(--amber-lite);
  background-image: var(--pix), linear-gradient(155deg, var(--amber-lite) 0%, #FFF6EA 100%);
  background-repeat: no-repeat, no-repeat;
  background-position: center, center;
  /* 【定值不用百分比】。四张图的墨迹占格不同（8×13 到 12×9），
     按百分比缩放出来一张细高一张扁宽，差 50% —— 正是头像统一画布之前
     的那个毛病。96rpx = 48px = 16 格 × 3px，DPR2 下每格恰好 6 个设备像素，
     像素块才齐。 */
  background-size: 96rpx auto, cover;
  image-rendering: pixelated;
}
.empty-t { font-size: 33rpx; font-weight: 600; color: var(--ink); }
.empty-s { font-size: 25rpx; color: var(--ink-faint); margin-top: 12rpx; line-height: 1.7; max-width: 540rpx; }
.empty-state .btn { margin-top: 36rpx; }


/* ── 骨架屏（0830 新增）──────────────────────────────────────
   「取着……」四个字是廉价感的头号来源：白屏一闪，然后内容砸下来。
   骨架屏把那一秒变成「东西正在到位」而不是「什么都没有」。

   两条讲究：
   ① 形状要跟真内容【对得上】—— 对不上的话内容一来就跳一下，比转圈还差
   ② 呼吸要慢（1.4s）。快了像在闪，那是焦虑不是等待

   不用转圈。转圈说的是「系统在忙」，骨架说的是「你的东西在路上」。 */
.sk { background: var(--paper-inset); border-radius: 10rpx; animation: sk-breathe 1.4s ease-in-out infinite; }
@keyframes sk-breathe { 0%, 100% { opacity: 1 } 50% { opacity: .45 } }
.sk-line { height: 26rpx; }
.sk-line.short { width: 38% }
.sk-line.mid   { width: 62% }
.sk-line.long  { width: 88% }
.sk-face { width: 84rpx; height: 84rpx; border-radius: 999rpx; flex: none; }
.sk-card {
  background: var(--paper-panel); border-radius: var(--r-card);
  box-shadow: var(--sh-card); padding: 22rpx 24rpx; margin-bottom: 14rpx;
  display: flex; gap: 20rpx; align-items: center;
}
.sk-card .sk-body { flex: 1; }
.sk-card .sk-line + .sk-line { margin-top: 12rpx; }
.sk-rows { padding-top: 8rpx; }


/* ── 输入框（0830 新增全局）──────────────────────────────────
   原先每一页各写各的，多数是「白底 + 1rpx 深边框」—— 那是网页表单的样子，
   在暖底上尤其硬。这一版统一成【凹陷】：浅色底、无边框、圆角。
   凹下去的东西看起来「可以往里写」，框起来的东西看起来「只能读」。 */
input, textarea, .field-input {
  background: var(--paper-inset);
  border: none;
  border-radius: 18rpx;
  padding: 20rpx 24rpx;
  font-size: 28rpx;
  color: var(--ink);
  box-sizing: border-box;
}
input::placeholder, textarea::placeholder { color: var(--stone-label); }


/* ── 列表逐个入场（0830）────────────────────────────────────
   一整屏内容【一起】出现是「数据到了」；一个接一个出现是「东西正在摆上来」。
   差别只有两百毫秒，但后者是游戏，前者是表格。

   每项差 40ms：五项共 200ms，有节奏而不拖。
   超过 60ms 就开始等 —— 那时它从「有生气」变成「慢」。

   翻页时会再跑一遍，那正好：翻页本来就该有翻的感觉。 */
.enter { animation: enter .34s cubic-bezier(.22,1,.36,1) both; }
@keyframes enter {
  0%   { transform: translateY(14rpx); opacity: 0 }
  100% { transform: translateY(0);     opacity: 1 }
}


/* 头像的默认底色。**必须写在这里，不能写在页面里** ——
   页面样式排在 app.wxss 之后，在那儿写 background 会把下面七条方向色
   全部盖掉，症状是四十个人共用一个颜色（看着就像方向色没做）。
   四柱那条色带踩过同一个坑。 */
.says-face, .item-face, .face, .glyph {
  background: linear-gradient(150deg, var(--amber-lite), #FFC489);
  color: #8A5A1E;
}

/* ── 头像的七种底色（0830）──────────────────────────────────
   四十位共用一个琥珀圆牌时，翻名册一眼分不出谁是谁 —— 而这个产品
   立身之本就是「四十个各不相同的人」。

   配色不是随机分的，按 `lack_bias` 的【方向】走：
   缺勤的催你动、缺静的劝你停、缺亲人的往人里去 —— 同一路人同一个色，
   所以颜色本身也在说事。

   等 40 张头像画好，这一整套连同 `.face-*` 一起撤掉。 */
.face-move   { --f1: #FFE0C2; --f2: #FFB067; background-color: var(--f1); background-image: linear-gradient(150deg, var(--f1), var(--f2)); color: #8A4A12; }
.face-still  { --f1: #DCEEF7; --f2: #9CC9E4; background-color: var(--f1); background-image: linear-gradient(150deg, var(--f1), var(--f2)); color: #2A5C7A; }
.face-wait   { --f1: #E6E2F5; --f2: #B3AADD; background-color: var(--f1); background-image: linear-gradient(150deg, var(--f1), var(--f2)); color: #4A3F7A; }
.face-keep   { --f1: #E3EDDC; --f2: #A9C99A; background-color: var(--f1); background-image: linear-gradient(150deg, var(--f1), var(--f2)); color: #3F5F31; }
.face-let_go { --f1: #F8DFE4; --f2: #E7A8B8; background-color: var(--f1); background-image: linear-gradient(150deg, var(--f1), var(--f2)); color: #7A3448; }
.face-near   { --f1: #FFE6D2; --f2: #F5A98C; background-color: var(--f1); background-image: linear-gradient(150deg, var(--f1), var(--f2)); color: #8A3F28; }
.face-ask    { --f1: #FDEDCB; --f2: #E8C878; background-color: var(--f1); background-image: linear-gradient(150deg, var(--f1), var(--f2)); color: #7A5A1A; }

/* 【铺了真脸的圆牌】。四十位的正面像素图早就画好在设计册里，
   只是从来没接到这一侧 —— 界面上一直是「圆底 + 姓名末字」。
   由 `rooms/tools/export-faces.mjs` 导出成 engine/faces.js，
   `utils/face.ts` 的 `脸(id)` 拼出这一段 style。

   `pixelated` 是必须的:14×16 的图放到 168rpx，不声明就被插值成一团糊。
   底色留着 —— 像素图有透明处，透出方向色，跟还没画好的那些看着是一套。

   【2026-09-01】导出那一步开始把四十张摊到同一块 14×16 的画布上
   （宽居中、底对齐）。在那之前每张各是各的尺寸（11×11 到 14×16），
   同一个 74% 缩放下，窄的那位被放得更大 —— 并排摆着像素块一个人一个
   大小，看着不是一套画。画布统一之后这一档从 74% 提到 84%:
   人物在圆牌里的占比才跟原先相当（画布宽了两格，人没变大）。 */
/* 【底盘要用 background-color 画】。方向色原先写成
   `background: linear-gradient(...)` —— 简写把渐变放进 background-image，
   而 `utils/face.ts` 内联的 `background-image:url(...)` 把它整个顶掉。
   于是四十位像素人从头到尾【裸浮在纸上】:截图取色，婆婆圆牌里是
   #FFFCF6，正是页面纸色（2026-09-01 五路评审抓到）。
   方向色这套编码——「颜色本身在说他劝你往哪走」——一次都没出现过。
   下面每个 `.face-<方向>` 因此都补了 `background-color`:
   有画的人，画压在这块实色上;没画的人，渐变照旧。 */
/* 【脸不许被圆切掉】（2026-09-01 第二轮评审 · 视觉）。
   上一版是 `84% / center 58%` —— 阿云的斗笠帽檐两端被圆形切成两个平口，
   读起来像渲染坏了，而不像裁剪。

   尺寸是【量出来的】，不是调出来的:四十张脸都摊在 14×16 的画布上
   （export-faces.mjs 统一画布、宽度居中、底边对齐），逐行取不透明像素的
   最左最右，再二分求「每一张的每一个墨点都落在圆内」的最大宽度。
   竖直居中时是 70.0%（几何上限:14×16 的对角线要塞进直径里，
   14/√(14²+16²) = 65.7%，比它松一点是因为四角本来没有墨）；
   而按原先的 `center 58%` 往下压，上限只剩 60.7%。
   取 68%:居中，留两个百分点余量。

   【代价与更好的那条路】。68% 比原先的 84% 明显小一圈，圆里空出一环 ——
   这是几何决定的，不是调参能绕过的:14×16 的画布整个塞进圆里就只有这么大。
   真正两全的做法是【让半身像破框】:圆牌退成背景，脸画在它上面、
   允许探出去（游戏 UI 常见）。那需要把脸从 `background-image` 挪到一个
   不被圆角裁的伪元素上（`脸()` 改成给 `--face` 变量，CSS 用 `var(--face)`），
   十四处 wxml 一个字都不用动。留给下一轮 —— 现在先把「被切平」这个
   实打实的缺陷修掉。 */
.face-art {
  background-repeat: no-repeat;
  background-position: center center;
  background-size: 68% auto;
  image-rendering: pixelated;
}

/* 【同一路人别长成同一张脸】。方向色是有意的（颜色本身在说「他劝你往哪走」），
   不能打乱;但缺「近人」那一路有十一位，名册上十一个一模一样的粉橙圆牌
   并排摆着，占位感最重的就是这一屏。

   同色之内换渐变角度 —— 六档，够把十一位分开，颜色仍然在说方向。
   类名由村民 id 定（`utils/face.ts` 的 `脸纹()`），同一个人在
   名册、村子、他的主页、搬进来那一刻都是同一张脸。
   等四十张立绘画好，这一套连同 `.face-*` 一起撤掉。 */
.fa-0 { background-image: linear-gradient(150deg, var(--f1), var(--f2)) }
.fa-1 { background-image: linear-gradient(30deg,  var(--f1), var(--f2)) }
.fa-2 { background-image: linear-gradient(200deg, var(--f1), var(--f2)) }
.fa-3 { background-image: linear-gradient(95deg,  var(--f1), var(--f2)) }
.fa-4 { background-image: linear-gradient(255deg, var(--f1), var(--f2)) }
.fa-5 { background-image: linear-gradient(325deg, var(--f1), var(--f2)) }

/* 「缺什么」那颗药丸跟头像走同一个方向色。
   原先它固定是琥珀 —— 于是丹增的卡上，头像是蓝的（缺静 · 劝你停），
   药丸却是橙的，同一件事在一张卡上用两种颜色说了两遍。
   翻名册时颜色本来是最快的那条线索，两套颜色等于没有线索。

   底色比头像浅一档：药丸是标签，不该跟头像抢。
   跟 `.face-*` 放在一起是刻意的 —— 拆开摆迟早有一边先改。 */
.item-lack   { background: #FFEEDF; color: #8A4A12; }   /* 没方向时的兜底 */
.lack-move   { background: #FFEEDF; color: #8A4A12; }
.lack-still  { background: #E7F3F9; color: #2A5C7A; }
.lack-wait   { background: #EFECF9; color: #4A3F7A; }
.lack-keep   { background: #EDF4E8; color: #3F5F31; }
.lack-let_go { background: #FCECEF; color: #7A3448; }
.lack-near   { background: #FFF0E6; color: #8A3F28; }
.lack-ask    { background: #FEF5E2; color: #7A5A1A; }

/* ── 「有人在说话」那张卡（0830 标尺 §1.5.2 沉浸感）──────────────
   村主屏的「他今天说」、首屏的开场白、填出生时间与算完那两屏的旁白
   —— 四处同一张卡、同一个位置感。摊在各页各写一份必然走散，
   而走散的样子是「同一件东西在不同屏上长得不一样」，正好毁掉沉浸感。

   底色由 `.face-<方向>` 给（住着的那位用他自己的颜色）；
   村子自己说话时用 `.intro-face` 的绿 —— 那是「地方」的颜色，不是某个人的。 */
.says {
  align-self: stretch;
  margin: 0 0 12rpx;
  padding: 20rpx 22rpx;
  background: var(--paper-panel);
  border-radius: var(--r-card);
  box-shadow: var(--sh-card);
  display: flex; gap: 18rpx; align-items: flex-start;
}
.says-hover { transform: scale(.985); }
.says-face {
  flex: none;
  width: 84rpx; height: 84rpx; border-radius: 999rpx;
  font-size: 38rpx; font-weight: 700;
  display: flex; align-items: center; justify-content: center;
}
.says-body { flex: 1; min-width: 0; }
/* 【四处手写同一块气泡，量过了，暂不抽】（2026-09-02）。
   第二轮评审说「.says 有四份实现」。实情:样式全在这儿（十条规则，
   一份），重复的只是 wxml 那六行结构，一共四处（村主屏两处、
   建生辰两处），而且逐字比过 —— 结构完全一致，没有漂。

   抽成 `<template>` 要先给镜像的垫片加 `<template>` / `<import>` 支持，
   而铁律一是「垫片遇到不认识的东西必须抛」—— 加这个能力有它自己的
   风险面，换来的只是四处六行的去重，而真正会漂的那一半（样式）
   本来就已经是一份了。
   等它真的漂了、或者第五处出现时再抽。 */
.says-who { font-size: 23rpx; color: var(--ink-faint); }
.says-text { font-size: 33rpx; font-weight: 600; color: var(--ink); line-height: 1.5; margin-top: 4rpx; }
.says-foot { display: flex; align-items: baseline; margin-top: 10rpx; }
.says-id { font-size: 21rpx; color: var(--stone-label); }
.says-go { margin-left: auto; font-size: 24rpx; color: var(--amber-text); font-weight: 600; }
/* 村子自己说话时的脸 —— 绿是「这个地方」的颜色，跟四十个人的七色分得开 */
.intro-face { background: linear-gradient(150deg, var(--moss-lite), #9BD9BA); color: #1F5C40; }
.intro-alt { color: var(--amber-text); font-weight: 600; }
