.wrap { display: flex; flex-direction: column; align-items: center;
        background: var(--paper); min-height: 100vh; }
/* 舞台吃掉纵向富余,画布整幅居中放在里面(尺寸由 index.ts 算,理由写在那儿)。 */
.stage { flex: 1 1 auto; min-height: 0; width: 100%;
         display: flex; align-items: center; justify-content: center; }
.room { display: block; }
/* 高度写死:它直接决定画布能有多高(见 index.ts 里那段)。
   随内容浮动的话,多一颗「收课」按钮就少一截房间。 */
.bar { display: flex; align-items: center; gap: 24rpx; height: 96rpx;
       padding: 0 32rpx; width: 100%; box-sizing: border-box; flex: 0 0 auto; }
.who { font-size: 30rpx; color: var(--ink); }
/* 【2026-09-01 从近黑换成琥珀】。这是六间屋子底下唯一那颗动作按钮，
   而它原先是 `--ink`（#2B2620）—— 一个黑方块。全 app 的主按钮都是
   琥珀 + 14px 圆角 + 6rpx 实心厚度，只有这里是另一个物种，
   于是六间最花力气的房间在视觉上跟这个 app 的其余部分是断开的
   （2026-09-01 五路评审 · 视觉）。
   暗色房间里琥珀比黑方块可读得多 —— 黑压在暗木地板上几乎没有边界。 */
/* 表演按钮同理:实测 118×36，也在 44 以下。 */
.cast { margin-left: auto; padding: 0 34rpx; min-height: 88rpx;
        display: inline-flex; align-items: center; border-radius: 28rpx;
        background: var(--amber); color: var(--on-amber); font-size: 28rpx; font-weight: 600;
        box-shadow: 0 4rpx 0 var(--amber-deep), 0 6rpx 16rpx rgba(232,121,26,.28); }
.err { padding: 32rpx; color: var(--peach-text); font-size: 26rpx; line-height: 1.7; }

/* 出口。名字左边，跟别处的「回去」同一档次要动作。
   【2026-09-01】它压在纸色底栏上，但那一条底栏挨着深色房间 ——
   给它一块浅底，别让它成为深色画面边上的一行浅灰字。 */
/* 【按得着】。实测 66×29（CSS px）—— 而它是这一屏【唯一】的出口。
   触达面积的通行下限是 44×44（iOS HIG / WCAG 2.5.5），29 差得远:
   在深色房间边上点不中它，人会以为退不出去
   （2026-09-01 第二轮评审 · 视觉量出来的）。
   88rpx = 44px。字号与配色不动，只把可点的那块撑到该有的大小。 */
.back { font-size: 26rpx; color: var(--ink-mid); padding: 0 24rpx;
        min-height: 88rpx; display: inline-flex; align-items: center;
        background: var(--paper-inset); border-radius: 999rpx; }
