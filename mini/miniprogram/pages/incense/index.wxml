<view class="page">
  <view class="hd">
    <view class="title">苏合配的那一味</view>
    <view class="sub">香药娘子 · 岭南 · 她缺「暖」</view>
  </view>

  <!-- 她按你缺的那一味说话。没有本命时不编一句 ——
       「你缺火」是算出来的，没算过就说没算过，并给出口（设计 10.7）。 -->
  <view class="say" wx:if="{{line}}">「{{line}}」</view>
  <view class="say muted" wx:else>
    <text>「你缺什么，我还不知道 —— 先把生辰填了，我才配得准」</text>
    <text class="say-go" bindtap="goNatal">去填生辰 ›</text>
  </view>

  <view class="recipe">乳香 · 安息 · 桂</view>

  <view class="loading" wx:if="{{loading}}">取着……</view>
  <view class="err" wx:elif="{{err}}">
    <view class="err-msg">{{err}}</view>
    <button class="btn ghost" bindtap="load">再试一次</button>
  </view>

  <view class="entries" wx:else>
    <view wx:for="{{skus}}" wx:key="id" class="entry"
          data-id="{{item.id}}" bindtap="onPick"
          hover-class="entry-hover" hover-stay-time="100">
      <text class="entry-k">{{item.name}}</text>
      <text class="entry-v">{{item.priceText}}</text>
      <text class="entry-arrow">›</text>
    </view>
  </view>

  <view class="note">一支烧二三十分钟 · 十支约够一个月</view>

  <!-- 弹性槽 · 矮屏收起。**这一句不是入口** —— 同步点香（设计 E1）还没做，
       做成可点的就是一条死链，而死链比没有更糟：它答应了一件做不到的事。 -->
  <view class="flexslot slot-e1">周四晚九点，村里一起点一支</view>

  <view class="acts">
    <button class="btn ghost" bindtap="onBack">回去</button>
  </view>
</view>
