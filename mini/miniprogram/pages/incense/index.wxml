<view class="page">
  <view class="hd">
    <view class="title">苏合配的那一味</view>
    <view class="sub">调香的 · 岭南 · 苏合缺「暖」</view>
  </view>

  <!-- 她按你缺的那一味说话。没有本命时不编一句 ——
       「你缺火」是算出来的，没算过就说没算过，并给出口（设计 10.7）。 -->
  <!-- 0830：她说话就该有脸。「东西长在卖它的人身上」是这个产品的论点，
       而这一屏原先只有一段引号里的灰字 —— 谁在说话看不出来。
       跟村子主屏、名字那一屏同一套：头像圆牌 + 气泡。 -->
  <view class="say-row">
    <!-- 苏合的方向是 near（往人里去）。这一屏就是她的，所以写死；
         但写的是她真正的那个色，不是在这儿现调一个 -->
    <view class="say-face face-near">苏</view>
    <view class="bubble">
      <block wx:if="{{line}}">{{line}}</block>
      <block wx:else>
        你缺什么，我还不知道 —— 先把出生时间填了，我才配得准
        <text class="say-go" bindtap="goNatal">去填出生时间 ›</text>
      </block>
    </view>
  </view>

  <view class="recipe">
    <text class="recipe-k">这一味</text>
    <text class="recipe-v">乳香 · 安息 · 桂</text>
  </view>

  <view class="sk-rows" wx:if="{{loading}}">
    <view class="sk-card"><view class="sk-body">
      <view class="sk sk-line mid"></view><view class="sk sk-line long"></view>
    </view></view>
  </view>
  <view class="err" wx:elif="{{err}}">
    <view class="err-msg">{{err}}</view>
    <button class="btn ghost" bindtap="load">再试一次</button>
  </view>

  <!-- 三档是【选一个】，不是一张目录。做成牌，价格跟名字一样大 ——
       它就是这一屏要你做的那个决定 -->
  <view class="picks" wx:else>
    <view wx:for="{{skus}}" wx:key="id" class="pick enter"
          style="animation-delay:{{index * 45}}ms"
          data-id="{{item.id}}" bindtap="onPick"
          hover-class="pick-hover" hover-stay-time="100">
      <view class="pick-n">{{item.name}}</view>
      <view class="pick-p">{{item.priceText}}</view>
      <!-- 点一档直接进确认屏（等于开始下单）。这条动作带做成实心的:
           原先是一行 21rpx 的橙字，比屏底那颗「回去」还轻 ——
           而这一屏最要紧的事是买。三张牌上写同一句「买这一档」也没必要，
           上面已经写着这一档是什么、多少钱。 -->
      <view class="pick-go">买</view>
    </view>
  </view>

  <view class="note">一支烧二三十分钟 · 十支约够一个月</view>

  <!-- 弹性槽 · 矮屏收起。**到点了才是入口** —— 不到点它只是一句话。
       一条常年可点、点进去说「还没开始」的链接，等于把设计册明说不要做的
       那张占位页换个地方摆出来。 -->
  <view wx:if="{{tonight}}" class="flexslot slot-e1 slot-on"
        bindtap="goTonight" hover-class="entry-hover" hover-stay-time="100">
    <text>今晚九点已经开始了，一起点一支</text><text class="slot-arrow">›</text>
  </view>
  <view wx:else class="flexslot slot-e1">周四晚九点，村里一起点一支</view>

  <view class="acts">
    <button class="btn ghost" bindtap="onBack">回去</button>
  </view>
</view>
