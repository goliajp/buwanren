<view class="page">
  <!-- 御守是【一个人】。这一屏是决定掏不掏钱的地方，而它原先的标题是
       「一件 / 御守」—— 刚点的明明是「丹增 · 请回村」，落地却是一件匿名商品，
       连说明里都只能写「这位不完人」。标尺 §1.5.3 第一问「我在哪儿」答不上。
       现在：是谁买谁，脸和名字在最上面；不是御守的商品（香、报告）照旧。 -->
  <view class="hd who" wx:if="{{villager && fulfillment === 'residency'}}">
    <view class="who-face product-face face-{{villager.direction}} {{脸样 ? 'face-art' : ''}}" style="{{脸样}}"><text wx:if="{{!脸样}}">{{villager.face}}</text></view>
    <view class="who-body">
      <!-- 不带代词：四十位里男女都有，写死一个「他」轮到婆婆那天就是错的
           （0830 的代词清扫定过这条，而这一行是后加的，差点又犯一次） -->
      <view class="eyebrow">请回村</view>
      <view class="title">{{villager.name}}</view>
      <view class="who-sub" wx:if="{{villager.title}}">{{villager.title}}<text wx:if="{{villager.art}}"> · {{villager.art}}</text></view>
    </view>
  </view>
  <!-- 挂着人、但买了不会住进来的（香、玉坠这些）:人还是要留在屏上 ——
       这一味是她配的，那是买它的理由。只是标题得是【东西】，
       不能写成「苏合的御守」让人以为请了个人回来。
       眉标不带代词:四十位里男女都有。 -->
  <view class="hd who" wx:elif="{{villager}}">
    <view class="who-face product-face face-{{villager.direction}} {{脸样 ? 'face-art' : ''}}" style="{{脸样}}"><text wx:if="{{!脸样}}">{{villager.face}}</text></view>
    <view class="who-body">
      <view class="eyebrow">{{villager.name}}那儿的</view>
      <view class="title">{{name || '　'}}</view>
      <view class="who-sub" wx:if="{{villager.title}}">{{villager.title}}<text wx:if="{{villager.art}}"> · {{villager.art}}</text></view>
    </view>
  </view>
  <view class="hd" wx:else>
    <view class="eyebrow">一件</view>
    <view class="title">{{name || '　'}}</view>
  </view>
  <view class="hr"></view>

  <view class="sk-rows" wx:if="{{loading}}">
    <view class="sk-card"><view class="sk-body">
      <view class="sk sk-line mid"></view><view class="sk sk-line long"></view>
    </view></view>
  </view>

  <view class="err" wx:elif="{{err}}">
    <view class="err-msg">{{err}}</view>
    <button class="btn ghost" bindtap="load">再试一次</button>
  </view>

  <block wx:else>
    <view class="sub" wx:if="{{subTitle}}">{{subTitle}}</view>

    <!-- 【实物要有实物的样子】。¥29 的香、¥398 的玉坠是寄到家的东西，
         而这一屏原先唯一的图是店主的头像。有图才显 ——
         说明书那一档没有实物，给它配张图反而是在暗示会寄东西给你。 -->
    <view class="goods" wx:if="{{货图}}">
      <image class="goods-img" src="{{货图}}" mode="aspectFit"></image>
    </view>

    <!-- 【那个 else 挂错了枝】（2026-09-06）。它原先挂在 `档名` 上 ——
         也就是说【只有一档价的商品】会把「这个区暂时没有定价」
         贴在价钱旁边:玉坠那一屏上印的是「¥398这个区暂时没有定价」,
         御守那一屏是「¥99这个区暂时没有定价」。
         两句话彼此矛盾，而它们并排站着。
         这一条在截屏里躺了不知道多久 —— 图截到了，文字也记进了
         measure.json，只是没有一支门禁读那份数据（同日补了 check-fold）。
         没有价才是「没有定价」，所以 else 要挂在 `price` 上。 -->
    <!-- 【三张牌就是价钱，上面不再摆一遍】（2026-09-06）。
         香那一屏底下摆着三张牌（试香 ¥29 / 一盒十支 ¥88 / 单配 ¥268），
         选中的那张描着墨边、价是主色 —— 而屏顶又摆一个大大的「¥29 试香 · 三支」。
         同样的两条信息，隔着 300px 出现两次。
         一档的时候（玉坠、御守、说明书）大价钱是这一屏的锚，留着;
         多档的时候锚就是那一排牌。 -->
    <view class="price-row" wx:if="{{!多档}}">
      <block wx:if="{{price}}">
        <text class="price">{{price}}</text>
        <!-- 【它是按月的这件事，要跟价钱写在一起】（2026-09-06）。
             订阅那一件在这儿原先是个光秃秃的「¥78」，旁边货架上还摆着
             「一盒十支 ¥88」—— 读起来就是「更便宜的一盒」。 -->
        <text class="price-cycle" wx:if="{{订阅}}"> / 月</text>
      </block>
      <text class="price-none" wx:else>这个区暂时没有定价</text>
    </view>

    <!-- 【标签撤了】（2026-09-06）。原先这儿摆 `product.tags` ——
         香那一屏是「香 / 苏合 / 实物」，御守那一屏是「御守 / 村民」。
         那是运营给商品打的分类标记，不是买家做决定要看的东西:
         「实物」下面那行「实物，会寄给你」已经说了，「苏合」头上那张脸
         已经说了，「御守」整屏都在说。三个灰圈占掉 46px,
         而这两屏正好各超出一屏 95px 与 64px。 -->
    <view class="desc" wx:if="{{desc}}">{{desc}}</view>

    <!-- 【你会收到什么】。这是花这笔钱的决策屏，而它原先只有一行小灰字
         把三件事挤在一起（一枚实物 / 扫开住进来 / 从此每天一句），
         底下半屏空着。摊开三条 —— 买之前该知道的就这三件。 -->
    <view class="gets" wx:if="{{fulfillment === 'residency' && villager}}">
      <view class="gets-k">你会收到</view>
    <!-- 【三步说的是今天真会发生的事】（2026-09-01 第二轮评审 · 转化路）。
         上一版写的是「请一位 › 收到护身符 › 扫开它」——
         而后端在【付款那一刻】就让他搬进村里了（fulfillment.rs 的
         residency 分支直接 move_in，既不建包裹也不发凭据）。
         那三步一步都不会发生:没有东西寄出，没有东西可扫，
         订单进度条永远停在「寄出」。
         按今天真做的事说:付完他就搬进来。
         扫护身符那条路仍然在（村子右上角），那是线下拿到实体御守的人走的。 -->
      <view class="gets-row">
        <view class="gets-n">1</view>
        <view class="gets-b">
          <view class="gets-t">{{villager.name}}搬进你的村子</view>
          <view class="gets-s">四十间屋子里的那一间，从此有人</view>
        </view>
      </view>
      <view class="gets-row">
        <view class="gets-n">2</view>
        <view class="gets-b">
          <view class="gets-t">付完就能去{{villager.name}}屋里</view>
          <view class="gets-s">不用等，也不用等谁寄</view>
        </view>
      </view>
      <view class="gets-row">
        <view class="gets-n">3</view>
        <view class="gets-b">
          <view class="gets-t">每天跟你说一句</view>
          <view class="gets-s">也能替你看一件事</view>
        </view>
      </view>
    </view>
    <!-- 【说今天真会发生的事】。这句原先写「寄一枚御守到你手上 · 扫开它，
         人就住进你的村子」—— 而后端在【付款那一刻】就让他住进来了
         （fulfillment.rs 的 residency 分支直接 move_in），没有包裹、
         没有单号、没有要扫的东西。一整条转化路上每一屏都在许一个
         不会发生的诺（2026-09-01 第二轮评审 · 转化路）。
         扫护身符那条路仍然在，那是【线下拿到实体御守的人】走的，
         入口在村子右上角，跟买这件事无关。 -->
    <view class="ship" wx:elif="{{fulfillment === 'residency'}}">
      付完就搬进你的村子 · 有屋子、有名字、每天说一句
    </view>
    <!-- 描述里已经说过怎么寄的，这一行就不说了 —— 玉那一件的描述是
         「实物，顺丰寄给你，三到五天到」，底下再来一句「实物，会寄给你」:
         同一件事说两遍，而且后一句更含糊（2026-09-02）。 -->
    <view class="ship" wx:elif="{{fulfillment === 'shipping' && !描述里说了寄}}">实物，会寄给你</view>
    <view class="ship" wx:elif="{{fulfillment === 'async_compute'}}">下单后算，算完在这里看</view>

    <view class="note" wx:if="{{note}}">{{note}}</view>

    <!-- 【几档就摆几档】（2026-09-06 · 五路体验走查）。这一屏原先只摆一个价 ——
         香那一件于是永远显示 ¥29，而正文写着「十支约够一个月」，
         ¥29 买到的是三支。三档此前只有从苏合屋里进去才看得见:
         同一件商品，两条路进来看到的是两个不同的东西。
         跟苏合那一屏同一套牌面（`.picks` / `.pick`），只有一档时不摆。 -->
    <view class="picks" wx:if="{{档.length > 1}}">
      <view wx:for="{{档}}" wx:key="id"
            class="pick {{skuId === item.id ? 'pick-on' : ''}}"
            data-id="{{item.id}}" bindtap="挑一档"
            hover-class="pick-hover" hover-stay-time="80">
        <view class="pick-n">{{item.name}}</view>
        <view class="pick-p">{{item.priceText}}</view>
      </view>
    </view>

    <view class="cta">
      <!-- 「买」不再是下单，是去确认那一屏（R5 · P2）—— 文案跟着改，
           不然按钮说的跟它做的不是一回事 -->
      <button class="btn" bindtap="onBuy" disabled="{{!skuId}}">
        {{skuId ? 买法 : '暂时买不了'}}
      </button>
      <button class="btn ghost" bindtap="onBack">回去</button>
    </view>
    <!-- 【这句话指的动线不存在】（2026-09-06 三路验证 · 第一次打开的人）。
         原先写的是「下单后在「我的」里付款」—— 而真实动线是
         这一屏 → 确认屏 → `redirectTo` 订单屏，**一次都不经过「我的」**。
         照实说:下一步是确认，付款在微信里完成。 -->
    <!-- 【不写「每月扣一次」】（2026-09-07）。这个渠道没有免密代扣
         （`unmei-wx` 自己写着 `off_session_charge: false`），我们从来
         没有、也扣不了 —— 每一期是我们开一张单、你来付。
         写「自动扣」既是假话，也让怕被套牢的人白担一次心。 -->
    <view class="foot-hint" wx:if="{{订阅}}">
      下一步确认订单 · 每月一盒，每期我们开单你来付 · 支付要在微信里完成
    </view>
    <view class="foot-hint" wx:else>下一步确认这一单 · 支付要在微信里完成</view>
  </block>
</view>
