-- 一笔订阅的同一个计费周期，只许开一张发票。
--
-- 在这之前 `renew_due` 不问「这一期是不是已经续过了」—— 它明说到期判断归调用方。
-- 连着调两次就是两张发票、两笔订单，也就是扣两次（2026-08-24 实测）。
-- 今天没出事只是因为 billing worker 单实例、顺序处理：
-- **是部署形态在兜底，不是代码在兜底**。多起一个实例、或者后台加一颗
-- 「立即续费」按钮，这就是一次重复扣款。
--
-- 应用层那一问加在了 `renew_due` 里（周期还没走完就返回 NotDue）。
-- 这道索引是同一件事交给数据库再说一遍：并发下两个事务读到同一个
-- current_period_end 时，只有一个插得进去。

-- 建索引之前先确认没有已经存在的重复。有的话这条迁移会失败 ——
-- 那正是我们要的：库里真有重复扣款的记录时，不该被一条迁移悄悄抹平。
CREATE UNIQUE INDEX uq_sub_invoice_period ON subscription_invoice(subscription_id, period_start);
