-- 商品名里也不许有行话 · 2026-09-01
--
-- 「按你的八字单配」—— 八字是行话，而这是三档里最贵的一档:
-- 恰恰是最需要人看懂的地方，写着一个他没听过的词。
-- 界面那一支门禁只扫得到 wxml，这行字是数据库给的，从它眼皮底下过去了
-- （scripts/check-no-jargon.py 因此加了一条:上架商品名也要扫）。
--
-- 换成她实际做的事:按你缺的那一味配 —— 这一屏从头到尾说的就是这个
-- （「你缺什么，我还不知道 —— 先把出生时间填了，我才配得准」）。
UPDATE sku SET name = '按你缺的那味单配' WHERE id = 'sku-incense-bespoke';
