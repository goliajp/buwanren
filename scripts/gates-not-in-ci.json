{
  "_读法": "本机门禁里跑、而 CI 里【不跑】的那几支，每支都要写理由。「还没加」不算理由 —— 那是欠账不是决定：CI 不跑的门禁，谁不跑 gates.sh 就能绕过去。新出现一支没记在这里的，check-gates-in-ci 报红。",
  "本机独有": {
    "术数指的叶真存在吗": "要另一个仓库（goliajp/mingli）的 registry，CI 上没有那份源码。它读不到时会**明说跳过**，不会假装通过 —— 所以放进 CI 只会多一行跳过。",
    "check-punct · 文档标点": "2026-08-24 起文档不进 git（只有用于产物生成的东西进）。它要读的东西 CI 上根本不存在，留在 CI 里只会变成一支【够不着却报绿】的检查 —— 那比没有检查更糟。本机照跑。 它扫的是 md/html 文档（存量记账 2339 处）。产品文案那一支 check-punct-ui 不受影响，仍在 CI 里跑。",
    "设计文档里有没有悄悄失效的": "2026-08-24 起文档不进 git（只有用于产物生成的东西进）。它要读的东西 CI 上根本不存在，留在 CI 里只会变成一支【够不着却报绿】的检查 —— 那比没有检查更糟。本机照跑。 它读 .claude/design/*.html 里的 CSS 变量。",
    "线框每一屏都填满了吗": "2026-08-24 起文档不进 git（只有用于产物生成的东西进）。它要读的东西 CI 上根本不存在，留在 CI 里只会变成一支【够不着却报绿】的检查 —— 那比没有检查更糟。本机照跑。 它读 .claude/design/*.html 里的线框 SVG 坐标。"
  }
}
