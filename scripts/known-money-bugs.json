{
  "_读法": "已知的资金问题台账。每一条写着【现在是什么样】与【为什么还没修】。校验会真跑一遍复现：还是这样 → 打印一条 `·`（不计分，但每次都看得见）；不是这样了 → 红，说明行为变了，这一条该划掉或重写。跟 web/oversize-pages.json 同一套办法：允许有已知问题，但不允许它悄悄躺着。修法是产品决定，不是实现决定 —— 台账不替谁拍板。",
  "pay-channel-switch": {
    "现状": "换支付方式时（微信 jsapi → 微信 h5），旧的那一笔 pending 会被就地标成 expired，而渠道那一侧的下单可能已经发出去、用户仍然付得出去。这笔钱回来时 `apply_succeeded` 只认 pending/processing，expired 那一笔落进「渠道重推」那一支：`payment_event` 记下了这次回调，但 `channel_txn_id` 不写、订单不入账，接口回 200。",
    "跟原来那条什么关系": "原来那条是「同一张单连点两次就建两笔全额 pending」。2026-08-25 修掉了：第二次点「去支付」拿回的是同一笔，数据库那一侧由 `uq_payment_one_pending`（payment(order_id) WHERE status='pending'）兜底，绕开应用层直插第二笔也会被拒（实测报 duplicate key）。剩下的就是这一条 —— 触发条件从「手抖点两下」收窄到「明确地换一个渠道」。",
    "为什么不干脆不让换渠道": "换渠道是用户明确的动作，不是误触：微信付不了改支付宝，拒绝他等于让他等 30 分钟过期。真要收干净，得在作废旧那笔之前先去渠道撤单（close/cancel），而那要接每个渠道各自的撤单接口，且撤单本身会失败 —— 那是一件独立的事，不是这次改动的一部分。",
    "兜底在哪儿": "每日凌晨的对账 worker（unmei_app::recon）按 channel_txn_id 比对渠道账单。这笔的 channel_txn_id 是 NULL，所以渠道账单里那条会落进 missing_in_internal，进差异清单等人工处理 —— 钱不会静静丢掉，但要等到第二天、且要有人去处理那张清单。",
    "这里守的是什么": "守一条可验证的事实：同一张单上，同时最多只有一笔 pending，且待付合计不超过应付。它变了就红。换渠道这一支由 backend/unmei-app/tests/payment_refund_use_cases.rs 的 switching_channel_supersedes_the_old_pending_payment 钉着。",
    "复现于": "2026-08-16 首次（旧形态）；2026-08-23 走完整条链；2026-08-25 修掉主路，剩这一支未修。",
    "今天能换到哪些": "只有微信那四种模式（jsapi / mp / h5 / native）—— `PaymentAdapterRegistry::pick` 认不出别的，支付宝之类在进 `payment::start` 之前就被 400 拒掉了。所以现实里的触发形态是「小程序里发起了，人又去 H5 那一页」。接第二个真渠道那天，这一条的面会变宽。"
  }
}
